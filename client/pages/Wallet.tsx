import React from 'react'

function Wallet() {
    return (
        <div>Wallet: A section that displays the user's balance of the platform's native token or cryptocurrency, and allows them to manage their funds.</div>
    )
}

export default Wallet